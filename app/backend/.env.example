# This is an example file for local development.
# Copy this file to .env and fill in your actual credentials.
# DO NOT commit your .env file to version control.
# Sensitive values should be set as environment variables in your production/deployment environment.

GEMINI_API_KEY=YOUR_GEMINI_API_KEY
GITHUB_PAT=YOUR_GITHUB_PERSONAL_ACCESS_TOKEN # Required if you intend to use GitHub tools that need authentication
# The GITHUB_PAT is not directly used by this proxy, but it's a reminder for the tools it might call.
